In AVR Studio 4, create a new project and add source files in 'src' folder.  Also make sure to include library 'libm.a'.  In order to build code, make sure WinAVR is installed.

CheapStat_110310.hex programmed to flash
(If code is build, program 'default/CheapStat_v2.hex' to flash instead)
CheapStat_110310_EEPROM.hex programmed to EEPROM (after programming flash)

